# This is just a placeholder Chart.yaml for the values directory
# The actual chart is deployed from https://charts.rook.io/release
# This directory only contains values.yaml used by the target-chart factory
apiVersion: v2
name: rook-ceph-cluster-values
description: Values for Rook-Ceph cluster (not a standalone chart)
type: library
version: 0.1.0
