apiVersion: v2
name: cert-manager-config
description: Cert-Manager additional configuration (ClusterIssuers, Certificates, etc.)
type: application
version: 0.1.0
appVersion: "1.0"

keywords:
- cert-manager
- certificates
- letsencrypt
- configuration

annotations:
  category: Infrastructure
  platform.pn-infra.io/component: cert-manager-config
  platform.pn-infra.io/sync-wave: "1"
