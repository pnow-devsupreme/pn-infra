apiVersion: v2
name: external-dns-config
description: External-DNS additional configuration (ConfigMaps, DNS records, etc.)
type: application
version: 0.1.0
appVersion: "1.0"

keywords:
- external-dns
- dns
- cloudflare
- configuration

annotations:
  category: Infrastructure
  platform.pn-infra.io/component: external-dns-config
  platform.pn-infra.io/sync-wave: "1"
