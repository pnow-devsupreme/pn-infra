apiVersion: v2
name: argocd-config
description: ArgoCD additional configuration (Ingress, ConfigMaps, etc.)
type: application
version: 0.1.0
appVersion: "1.0"

keywords:
- argocd
- ingress
- configuration

annotations:
  category: GitOps
  platform.pn-infra.io/component: argocd-config
  platform.pn-infra.io/sync-wave: "1"
